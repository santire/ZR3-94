import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  title: {
    color: '#C800FA',
    fontWeight: 600,
  },
  views: {
    color: '#6D59C9',
    fontWeight: 600,
  },
  text: {
    textAlign: 'center',
    fontSize: 18,
    fontWeight: 400,
    color: '#F5F5F5',
  },
}));
