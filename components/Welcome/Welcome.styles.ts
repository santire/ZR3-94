import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
  content: {
    display: 'flex',
    flexDirection: 'column',
    maxWidth: 700,
    width: '75%',
    margin: '0 auto',
  },
}));
